{
	"require-dev": {
		"phpunit/phpunit": "7"
	},
	"require": {
		"twig/twig": "^2.0",
		"slim/slim": "^3.0"
	},
	"scripts": {
		"test": "phpunit --colors=always --verbose --bootstrap ./vendor/autoload.php tests/"
	}
}
